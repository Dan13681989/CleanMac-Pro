name: Homebrew Test

on:
  push:
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  test-intel:
    runs-on: macos-13
    steps:
      - name: Tap and install
        run: |
          HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew tap Dan13681989/cleanmac
          HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew install cleanmac-pro
          cleanmac version
          cleanmac help
  test-arm:
    runs-on: macos-14
    steps:
      - name: Tap and install
        run: |
          HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew tap Dan13681989/cleanmac
          HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew install cleanmac-pro
          cleanmac version
          cleanmac help
